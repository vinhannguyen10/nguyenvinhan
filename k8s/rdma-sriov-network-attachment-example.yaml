apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: rdma-sriov-example
  annotations:
    description: Example only. Requires SR-IOV Network Operator and site-specific resource names.
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "type": "sriov",
      "name": "rdma-sriov-example",
      "resourceName": "example.com/rdma"
    }
